# FogTap — rebuild preview

Clean Spark-bar rebuild. **Preview only.** Production alias stays on the holding page until Anchor ships.

## Visual bar
See `/workspace/fogtap/product/VISUAL_BAR.md` (source of truth).

- Hero / demo plate: single premium porch door (`media/door-clear.jpg`)
- Frost: CSS optical frost on the **same** door plate (glass region mask)
- No people stills, no collage, no Zac/Zoey paste-ups
- Optional cinematic B-roll (`fogtap-demo-v4.mp4`) deferred to keep deploy size small

## Stack
Static HTML/CSS/JS on Vercel. Waitlist: FormSubmit → `sideszac89@gmail.com` plus optional `/api/waitlist`.

## Branch
`rebuild-preview` on `trianguru/fogtap`
